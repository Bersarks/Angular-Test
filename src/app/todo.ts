export interface Todo { // This interface is used to define the structure of the objects in the array that is returned by the getTodoList() function in the TodoService.
	id: number;
	name: string;
	description: string;
}
